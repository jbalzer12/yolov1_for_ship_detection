# dataset images folder
#images: /scratch/tmp/jbalzer/data/airbus-ship-detection/train/images
images: data/airbus-ship-detection/train/images

# dataset labels folder
#labels: /scratch/tmp/jbalzer/data/airbus-ship-detection/train/labels
labels: data/airbus-ship-detection/train/labels

# class names
class_names: [ 'ship', 'empty' ]
